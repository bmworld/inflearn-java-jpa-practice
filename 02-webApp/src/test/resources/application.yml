# test > resources > application.yml 생성 시, TEST 코드 전용으로 실행됨
# datasource 설정이 없더라도, 자동으로 in-memory mode 작동함.

spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # ! create, update, none
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
